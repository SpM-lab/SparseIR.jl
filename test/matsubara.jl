@testset "matsubara.jl" begin
    @testset "single pole" begin
        for stat in instances(Statistics) for Λ in (1e1, 1e4)
            wmax = 1.1
            pole = 0.1 * wmax
            beta
        end
    end
end